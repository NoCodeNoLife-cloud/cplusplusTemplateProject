metadata:
  phase: EXECUTE
  bus_stage: COMMIT
  tier: cleanup
  project: cppforge
  branch: main

tasks:
  - id: cleanup-residuals
    title: "Clean up phase migration residuals"
    status: completed
    acceptance_criteria:
      - "Doxyfile deleted"
      - "CMakeLists.txt comment fixed"
      - "Relative include fixed"
    rollback_plan: "git checkout -- CMakeLists.txt ci/gtest/src/server/config/ConfigParamTest.cc; git checkout HEAD -- Doxyfile"

  - id: commit-cleanup
    title: "Commit cleanup changes"
    status: in_progress
    acceptance_criteria:
      - "All changes committed"
    rollback_plan: "git revert HEAD"
